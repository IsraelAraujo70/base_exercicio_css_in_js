import styled from 'styled-components'

const Cabec = styled.header`
  background-color: var(--cor-secundaria);
  color: var(--cor-principal);
  text-align: center;
  padding: 24px 0;
`
export default Cabec
